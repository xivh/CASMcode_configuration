[build-system]
requires = [
    "pip",
    "setuptools",
    "wheel",
    "pybind11>=2.8.0",
    "libcasm-global>=2.0.4",
    "libcasm-xtal>=2.0a9",
    "libcasm-clexulator>=2.0a4",
]
build-backend = "setuptools.build_meta"
